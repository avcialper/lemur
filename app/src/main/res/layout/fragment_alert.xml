<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Alert.Wrapper"
    tools:context=".ui.component.AlertFragment">

    <TextView
        android:id="@+id/text_label"
        style="@style/Alert.Content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/text_positive"
        style="@style/Alert.TextButton"
        android:text="@string/yes"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/text_label" />

    <TextView
        android:id="@+id/text_negative"
        style="@style/Alert.TextButton"
        android:text="@string/no"
        app:layout_constraintEnd_toStartOf="@id/text_positive"
        app:layout_constraintTop_toBottomOf="@id/text_label" />

</androidx.constraintlayout.widget.ConstraintLayout>